open import "std.bfr"

module Included = struct
  let x = 1
end

module A = struct
  let x = 2
  open Included

  let () = verify (x = 1)
end

module B = struct
  let x = 3
  include Included

  let () = verify (x = 1)
end

let main = $

;verify (A:x = 2)
;verify (B:x = 1)

*gen_puts "OK"